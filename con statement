// SPDX-License-Identifier: MIT
pragma solidity 0.8.28;

contract EvenOddcheck {
    // Function to check if a number is even
    function isEven(uint number) public pure returns (bool) {
        // Using the modulo operator to check for even or odd
        if (number % 2 == 0) {
            return true; // This is even number
        } else {
            return false; // This is odd number
        }
    }
}
<!---
kola-jpg/kola-jpg is a ✨ special ✨ repository because its `README.md` (this file) appears on your GitHub profile.
You can click the Preview link to take a look at your changes.
--->
